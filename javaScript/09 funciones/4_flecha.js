'use stricts'

 var suma = (num1,num2) => {
    return (num1 + num2)
};

console.log(suma(4, 5));


//---------------------------------

let saludar = nombre => console.log(`Hola ${nombre} como estas?`);

saludar('Mao');